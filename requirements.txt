# PlainCents — pinned dependencies
# Re-pinned in V2 Build Plan Phase 0 from the project's own venv (venv/), which
# is the source of truth. The previous requirements.txt was UTF-16-encoded and
# would not `pip install -r` correctly; this file is plain UTF-8.
#
# Install: pip install -r requirements.txt
# (Sub-dependencies of fastapi/uvicorn/pytest are included explicitly so the
#  full environment is reproducible from this file alone, per TRD §16 /
#  ML Spec §19's reproducibility requirements — not because each one is
#  imported directly by PlainCents code.)

# -- V1 pipeline / ML --------------------------------------
pandas==3.0.1
numpy==2.4.2
scikit-learn==1.8.0
scipy==1.17.0
joblib==1.5.3
matplotlib==3.10.8
yfinance==1.2.0

# -- V2 backend (FastAPI) -----------------------------------
fastapi==0.141.1
uvicorn[standard]==0.52.4
python-multipart==0.0.32  # required by FastAPI's UploadFile/Form (Phase 4 import endpoint)
pydantic==2.13.5

# -- Testing --------------------------------------------------
pytest==9.0.3
httpx2==2.12.0  # required by starlette.testclient.TestClient (Phase 2 API tests)

# -- Transitive dependencies (pinned for reproducibility) ----
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.14.2
beautifulsoup4==4.14.3
certifi==2026.1.4
cffi==2.0.0
charset-normalizer==3.4.4
click==8.5.0
colorama==0.4.6
contourpy==1.3.3
curl_cffi==0.13.0
cycler==0.12.1
fonttools==4.61.1
frozendict==2.4.7
h11==0.16.0
httpcore2==2.12.0
httptools==0.8.0
idna==3.19
iniconfig==2.3.0
kiwisolver==1.4.9
multitasking==0.0.12
packaging==26.0
peewee==4.0.0
pillow==12.1.1
platformdirs==4.9.2
pluggy==1.6.0
protobuf==6.33.5
pycparser==3.0
pydantic_core==2.46.5
Pygments==2.21.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
python-dotenv==1.2.3
pytz==2025.2
PyYAML==6.0.3
requests==2.32.5
six==1.17.0
soupsieve==2.8.3
starlette==1.6.0
threadpoolctl==3.6.0
truststore==0.10.4
typing-inspection==0.4.4
typing_extensions==4.15.0
tzdata==2025.3
urllib3==2.6.3
watchfiles==1.2.0
websockets==16.0
